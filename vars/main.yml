---
# helper variables
nifi_mirror_base: "http://apache.mediamirrors.org/nifi/{{ nifi.major_version }}.{{ nifi.minor_version }}.{{ nifi.patch_version }}"
nifi_dir_name: "nifi-{{ nifi.major_version }}.{{ nifi.minor_version }}.{{ nifi.patch_version }}"
nifi_toolkit_dir_name: "nifi-toolkit-{{ nifi.major_version }}.{{ nifi.minor_version }}.{{ nifi.patch_version }}"
